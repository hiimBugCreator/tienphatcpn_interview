name: tienphatcpn_interview
version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  bloc: ^8.1.2
  equatable:
  flutter_bloc: ^8.1.3
  logger_beauty:
  shared_preferences:
  crypto: ^3.0.3
  cached_network_image:
  go_router:
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/sample_data/